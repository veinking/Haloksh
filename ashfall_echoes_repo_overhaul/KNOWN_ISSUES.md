# Known Issues

- Some legacy saves missing deep map metadata are recovered with a regenerated map segment.
- Combat log text can still become noisy in long relic/status chains.
- Clipboard copy fallback for debug report opens a modal on older browsers.
- Mobile Safari may still resize viewport when system text size is extreme.
- Future refactor: split `src/js/app.js` into state/combat/ui modules.
